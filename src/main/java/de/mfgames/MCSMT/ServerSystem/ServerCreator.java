package de.mfgames.MCSMT.ServerSystem;

import java.nio.file.Path;
import java.nio.file.Paths;

public class ServerCreator {
    public static void CreateServer(String type, String path) {
        Path convPath = Paths.get(path);
    }
}
